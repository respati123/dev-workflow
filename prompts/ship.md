---
description: Drive a task end-to-end (issues -> per sub-issue, scout -> code -> PR -> review -> QA) — see the `ship` skill
argument-hint: "<task description | PRD path | issue number>"
---
Load and follow the `ship` skill (`skills/ship/SKILL.md` in this package) to
drive this task end to end: $ARGUMENTS
